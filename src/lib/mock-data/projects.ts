import { Project } from "../../types/project";

// Mock projects removed per Zero-Mock Data Policy.
// All project data must come from real student submissions via the database.
export const MOCK_PROJECTS: Project[] = [];
