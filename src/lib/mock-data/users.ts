import { Role } from "../constants/roles";

// Mock users removed per Zero-Mock Data Policy.
// All user data must come from the database or authenticated registration.
export const MOCK_USERS: {
  id: string;
  name: string;
  email: string;
  role: Role;
  avatar: string;
  collegeId: string;
  departmentId: string;
}[] = [];
